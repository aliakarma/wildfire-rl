# Geospatial preprocessing stack — ONLY needed to rebuild state tensors from
# raw remote-sensing sources (ERA5 / FIRMS / DEM / NDVI).
# GDAL-backed wheels are heavy; prefer conda (see environment.yml) on Windows.
-r requirements.txt

rasterio==1.3.10
rioxarray==0.15.5
xarray==2024.6.0
netCDF4==1.7.1
cdsapi==0.7.0
earthengine-api==0.1.408
geemap==0.32.1
scikit-image==0.24.0
