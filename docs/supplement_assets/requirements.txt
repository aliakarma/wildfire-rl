# Environment used to produce the frozen results in this archive.
# Linux (Ubuntu 22.04 / WSL2), Python 3.12.3.
#
# Only the first block is needed for the no-GPU verification paths (README steps 2 and 3).
# The training/simulator block is needed for the full reproduction path (README step 5).

# --- verification core (numbers, tables, statistics) ---------------------------------
numpy==1.26.4
pandas==2.3.3
scipy==1.17.1
matplotlib==3.11.0

# --- environment + training ----------------------------------------------------------
gymnasium==1.0.0
pettingzoo==1.26.1
torch==2.12.1
omegaconf==2.3.1
pyyaml==6.0.3
tqdm==4.68.3

# --- test suite ----------------------------------------------------------------------
pytest>=7.4

# Version ranges (rather than these exact pins) are declared in pyproject.toml; the pins
# above are what the frozen runs actually used. Per-run environment captures are recorded
# in the manifests under each wildfire_phase*/ directory.
