"""Policies (Phase 4+): information-matched heuristics, PPO baseline, MARL agents,
and the learned hierarchical strategic controller. Every policy reads only the
observation — no oracle state (the V1 privilege confound must not recur)."""
