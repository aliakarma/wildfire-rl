"""Reproducibility core: global seeding, run manifests, artifact hashing,
seed-integrity + tensor-invariant guards, and CSV-first report tables.

Ported from V1 in Phase 0 (import paths only). These utilities are the project's
credibility ace; their behavior is unchanged. See ``docs/MIGRATION.md``."""
